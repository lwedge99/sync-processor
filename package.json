{
  "name": "sync-processor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "sentio test",
    "gen": "sentio gen",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen"
  },
  "dependencies": {
    "@sentio/sdk": "^2.19.0",
    "@supercharge/promise-pool": "^2.4.0"
  },
  "devDependencies": {
    "@sentio/cli": "^2.9.2",
    "typescript": "^5.0.3"
  }
}
